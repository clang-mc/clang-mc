#pragma once

/* Generated by tools/commands/generator/generate.py. Do not edit this file. */

#include "bindings/CommandSupport.h"


#ifdef __cplusplus
extern "C" {
#endif

__asm__(
"export _ll_shared:z/libmc_cmd_effect_give:\n"
"    inline $execute store result score r0 vm_regs run effect give $(target) $(effect)\n"
"    ret\n"
"\n"
"export _ll_shared:z/libmc_cmd_effect_clear_all:\n"
"    inline $execute store result score r0 vm_regs run effect clear $(target)\n"
"    ret\n"
"\n"
"export _ll_shared:z/libmc_cmd_effect_clear:\n"
"    inline $execute store result score r0 vm_regs run effect clear $(target) $(effect)\n"
"    ret\n"
);

static inline int
effect_give_unsafe(McfStrRef target_ref, McfStrRef effect_ref)
{
    int ret;
    int target_slot;
    int effect_slot;

    target_slot = McfStrRef_SlotId(target_ref);
    effect_slot = McfStrRef_SlotId(effect_ref);
    if (target_slot < 0 || effect_slot < 0) {
        return -1;
    }

    __asm volatile (
        "inline data modify storage std:vm s6.cmd set value %{target: \"\", effect: \"\"%}\n"
        "inline $data modify storage std:vm s6.cmd.target set from storage std:vm mcstr.slots[%1].value\n"
        "inline $data modify storage std:vm s6.cmd.effect set from storage std:vm mcstr.slots[%2].value\n"
        "inline function _ll_shared:z/libmc_cmd_effect_give with storage std:vm s6.cmd\n"
        "inline scoreboard players operation %0 vm_regs = r0 vm_regs"
        : "=r"(ret)
        : "r"(target_slot), "r"(effect_slot)
    );
    return ret;
}

static inline int
effect_give(Target target, const Identifier * effect)
{
    int ret;
    McfStrRef target_ref;
    McfStrRef effect_ref;
    int target_slot;
    int effect_slot;

    target_ref = _Command_RequireTargetRef(target);
    effect_ref = _Command_RequireIdentifierRef(effect);
    target_slot = McfStrRef_SlotId(target_ref);
    effect_slot = McfStrRef_SlotId(effect_ref);
    if (target_slot < 0 || effect_slot < 0) {
        McfStrRef_Release(effect_ref);
        return -1;
    }

    ret = effect_give_unsafe(target_ref, effect_ref);
    McfStrRef_Release(effect_ref);
    return ret;
}

static inline int
effect_give_seconds_unsafe(McfStrRef target_ref, McfStrRef effect_ref, int seconds)
{
    int ret;
    int target_slot;
    int effect_slot;

    target_slot = McfStrRef_SlotId(target_ref);
    effect_slot = McfStrRef_SlotId(effect_ref);
    if (target_slot < 0 || effect_slot < 0) {
        return -1;
    }

    __asm volatile (
        "inline $data modify storage std:vm ls0.target set from storage std:vm mcstr.slots[%0].value\n"
        "inline $data modify storage std:vm ls0.effect set from storage std:vm mcstr.slots[%1].value"
        :
        : "r"(target_slot), "r"(effect_slot)
    );
    __asm volatile (
        "$$direct_args\n"
        "inline $execute store result score %0 vm_regs run effect give $(target) $(effect) %1"
        : "=r"(ret)
        : "r"(seconds)
    );
    return ret;
}

static inline int
effect_give_seconds(Target target, const Identifier * effect, int seconds)
{
    int ret;
    McfStrRef target_ref;
    McfStrRef effect_ref;
    int target_slot;
    int effect_slot;

    target_ref = _Command_RequireTargetRef(target);
    effect_ref = _Command_RequireIdentifierRef(effect);
    target_slot = McfStrRef_SlotId(target_ref);
    effect_slot = McfStrRef_SlotId(effect_ref);
    if (target_slot < 0 || effect_slot < 0) {
        McfStrRef_Release(effect_ref);
        return -1;
    }

    ret = effect_give_seconds_unsafe(target_ref, effect_ref, seconds);
    McfStrRef_Release(effect_ref);
    return ret;
}

static inline int
effect_give_amplified_unsafe(McfStrRef target_ref, McfStrRef effect_ref, int seconds, int amplifier)
{
    int ret;
    int target_slot;
    int effect_slot;

    target_slot = McfStrRef_SlotId(target_ref);
    effect_slot = McfStrRef_SlotId(effect_ref);
    if (target_slot < 0 || effect_slot < 0) {
        return -1;
    }

    __asm volatile (
        "inline $data modify storage std:vm ls0.target set from storage std:vm mcstr.slots[%0].value\n"
        "inline $data modify storage std:vm ls0.effect set from storage std:vm mcstr.slots[%1].value"
        :
        : "r"(target_slot), "r"(effect_slot)
    );
    __asm volatile (
        "$$direct_args\n"
        "inline $execute store result score %0 vm_regs run effect give $(target) $(effect) %1 %2"
        : "=r"(ret)
        : "r"(seconds), "r"(amplifier)
    );
    return ret;
}

static inline int
effect_give_amplified(Target target, const Identifier * effect, int seconds, int amplifier)
{
    int ret;
    McfStrRef target_ref;
    McfStrRef effect_ref;
    int target_slot;
    int effect_slot;

    target_ref = _Command_RequireTargetRef(target);
    effect_ref = _Command_RequireIdentifierRef(effect);
    target_slot = McfStrRef_SlotId(target_ref);
    effect_slot = McfStrRef_SlotId(effect_ref);
    if (target_slot < 0 || effect_slot < 0) {
        McfStrRef_Release(effect_ref);
        return -1;
    }

    ret = effect_give_amplified_unsafe(target_ref, effect_ref, seconds, amplifier);
    McfStrRef_Release(effect_ref);
    return ret;
}

static inline int
effect_give_hidden_unsafe(McfStrRef target_ref, McfStrRef effect_ref, int seconds, int amplifier, int hide_particles)
{
    int ret;
    int target_slot;
    int effect_slot;

    target_slot = McfStrRef_SlotId(target_ref);
    effect_slot = McfStrRef_SlotId(effect_ref);
    if (target_slot < 0 || effect_slot < 0) {
        return -1;
    }

    if (hide_particles) {
        __asm volatile (
            "inline $data modify storage std:vm ls0.target set from storage std:vm mcstr.slots[%0].value\n"
            "inline $data modify storage std:vm ls0.effect set from storage std:vm mcstr.slots[%1].value"
            :
            : "r"(target_slot), "r"(effect_slot)
        );
        __asm volatile (
            "$$direct_args\n"
            "inline $execute store result score %0 vm_regs run effect give $(target) $(effect) %1 %2 true"
            : "=r"(ret)
            : "r"(seconds), "r"(amplifier)
        );
        return ret;
    } else {
        __asm volatile (
            "inline $data modify storage std:vm ls0.target set from storage std:vm mcstr.slots[%0].value\n"
            "inline $data modify storage std:vm ls0.effect set from storage std:vm mcstr.slots[%1].value"
            :
            : "r"(target_slot), "r"(effect_slot)
        );
        __asm volatile (
            "$$direct_args\n"
            "inline $execute store result score %0 vm_regs run effect give $(target) $(effect) %1 %2 false"
            : "=r"(ret)
            : "r"(seconds), "r"(amplifier)
        );
        return ret;
    }
}

static inline int
effect_give_hidden(Target target, const Identifier * effect, int seconds, int amplifier, int hide_particles)
{
    int ret;
    McfStrRef target_ref;
    McfStrRef effect_ref;
    int target_slot;
    int effect_slot;

    target_ref = _Command_RequireTargetRef(target);
    effect_ref = _Command_RequireIdentifierRef(effect);
    target_slot = McfStrRef_SlotId(target_ref);
    effect_slot = McfStrRef_SlotId(effect_ref);
    if (target_slot < 0 || effect_slot < 0) {
        McfStrRef_Release(effect_ref);
        return -1;
    }

    ret = effect_give_hidden_unsafe(target_ref, effect_ref, seconds, amplifier, hide_particles);
    McfStrRef_Release(effect_ref);
    return ret;
}

static inline int
effect_clear_all_unsafe(McfStrRef target_ref)
{
    int ret;
    int target_slot;

    target_slot = McfStrRef_SlotId(target_ref);
    if (target_slot < 0) {
        return -1;
    }

    __asm volatile (
        "inline data modify storage std:vm s6.cmd set value %{target: \"\"%}\n"
        "inline $data modify storage std:vm s6.cmd.target set from storage std:vm mcstr.slots[%1].value\n"
        "inline function _ll_shared:z/libmc_cmd_effect_clear_all with storage std:vm s6.cmd\n"
        "inline scoreboard players operation %0 vm_regs = r0 vm_regs"
        : "=r"(ret)
        : "r"(target_slot)
    );
    return ret;
}

static inline int
effect_clear_all(Target target)
{
    McfStrRef target_ref;
    int target_slot;

    target_ref = _Command_RequireTargetRef(target);
    target_slot = McfStrRef_SlotId(target_ref);
    if (target_slot < 0) {
        return -1;
    }
    return effect_clear_all_unsafe(target_ref);
}

static inline int
effect_clear_unsafe(McfStrRef target_ref, McfStrRef effect_ref)
{
    int ret;
    int target_slot;
    int effect_slot;

    target_slot = McfStrRef_SlotId(target_ref);
    effect_slot = McfStrRef_SlotId(effect_ref);
    if (target_slot < 0 || effect_slot < 0) {
        return -1;
    }

    __asm volatile (
        "inline data modify storage std:vm s6.cmd set value %{target: \"\", effect: \"\"%}\n"
        "inline $data modify storage std:vm s6.cmd.target set from storage std:vm mcstr.slots[%1].value\n"
        "inline $data modify storage std:vm s6.cmd.effect set from storage std:vm mcstr.slots[%2].value\n"
        "inline function _ll_shared:z/libmc_cmd_effect_clear with storage std:vm s6.cmd\n"
        "inline scoreboard players operation %0 vm_regs = r0 vm_regs"
        : "=r"(ret)
        : "r"(target_slot), "r"(effect_slot)
    );
    return ret;
}

static inline int
effect_clear(Target target, const Identifier * effect)
{
    int ret;
    McfStrRef target_ref;
    McfStrRef effect_ref;
    int target_slot;
    int effect_slot;

    target_ref = _Command_RequireTargetRef(target);
    effect_ref = _Command_RequireIdentifierRef(effect);
    target_slot = McfStrRef_SlotId(target_ref);
    effect_slot = McfStrRef_SlotId(effect_ref);
    if (target_slot < 0 || effect_slot < 0) {
        McfStrRef_Release(effect_ref);
        return -1;
    }

    ret = effect_clear_unsafe(target_ref, effect_ref);
    McfStrRef_Release(effect_ref);
    return ret;
}

#ifdef __cplusplus
}
#endif
